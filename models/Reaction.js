const { Schema, Types } = require("mongoose");

const reactionSchema = new Schema(
  {
    // reactionId
    // reactionBody
    // username
    // createdAt
  },
  {}
);
module.exports = reactionSchema;
